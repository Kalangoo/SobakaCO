specVersion: "v1"
serviceId: "sobaka"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sobaka-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
